./riscv-template中为模拟器代码，Makefile为编译文件，elf为解析elf得到的文件。
./samples中为测试代码，包括lab2.1中的五个测试样例和本次lab2.2的十个测试样例。
./run.sh为测试单个样例的执行脚本，利用RISC-V工具链编译产生ELF文件，然后通过实现的模拟器运行。需要三个参数：测试代码文件名（代码位于samples中）、单步模式（1为单步模式）、处理器模式（0为单周期处理器，1为多周期处理器，2为流水线处理器）。
./statistic.sh为测试所有样例的执行脚本，不需要参数，分别以多周期处理器和流水线处理器的模式执行15个测试样例，不采用单步模式，打印执行结束的寄存器和内存状态，打印指令数、周期数、平均CPI、冒险数。
./result为执行statistic.sh脚本产生的结果。
./README为本文件。
./Lab2.2报告为本次Lab的实验报告。
